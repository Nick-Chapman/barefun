(*stage1*)
let _ = Unit0 in
let start = '@' in
let _ =
  let x =
    let x =
      let x = PRIM_CharOrd(start) in
      let x =
        let x =
          let x =
            let x =
              let x =
                let x =
                  let _ =
                    let x = PRIM_CharChr(x) in
                    PRIM_PutChar(x) in
                  let y = 1 in
                  PRIM_AddInt(x,y) in
                let _ =
                  let x = PRIM_CharChr(x) in
                  PRIM_PutChar(x) in
                let y = 1 in
                PRIM_AddInt(x,y) in
              let _ =
                let x = PRIM_CharChr(x) in
                PRIM_PutChar(x) in
              let y = 1 in
              PRIM_AddInt(x,y) in
            let x =
              let x =
                let _ =
                  let x = PRIM_CharChr(x) in
                  PRIM_PutChar(x) in
                let y = 1 in
                PRIM_AddInt(x,y) in
              let _ =
                let x = PRIM_CharChr(x) in
                PRIM_PutChar(x) in
              let y = 1 in
              PRIM_AddInt(x,y) in
            let _ =
              let x = PRIM_CharChr(x) in
              PRIM_PutChar(x) in
            let y = 1 in
            PRIM_AddInt(x,y) in
          let x =
            let x =
              let _ =
                let x = PRIM_CharChr(x) in
                PRIM_PutChar(x) in
              let y = 1 in
              PRIM_AddInt(x,y) in
            let _ =
              let x = PRIM_CharChr(x) in
              PRIM_PutChar(x) in
            let y = 1 in
            PRIM_AddInt(x,y) in
          let _ =
            let x = PRIM_CharChr(x) in
            PRIM_PutChar(x) in
          let y = 1 in
          PRIM_AddInt(x,y) in
        let x =
          let x =
            let x =
              let x =
                let _ =
                  let x = PRIM_CharChr(x) in
                  PRIM_PutChar(x) in
                let y = 1 in
                PRIM_AddInt(x,y) in
              let _ =
                let x = PRIM_CharChr(x) in
                PRIM_PutChar(x) in
              let y = 1 in
              PRIM_AddInt(x,y) in
            let _ =
              let x = PRIM_CharChr(x) in
              PRIM_PutChar(x) in
            let y = 1 in
            PRIM_AddInt(x,y) in
          let x =
            let x =
              let _ =
                let x = PRIM_CharChr(x) in
                PRIM_PutChar(x) in
              let y = 1 in
              PRIM_AddInt(x,y) in
            let _ =
              let x = PRIM_CharChr(x) in
              PRIM_PutChar(x) in
            let y = 1 in
            PRIM_AddInt(x,y) in
          let _ =
            let x = PRIM_CharChr(x) in
            PRIM_PutChar(x) in
          let y = 1 in
          PRIM_AddInt(x,y) in
        let x =
          let x =
            let _ =
              let x = PRIM_CharChr(x) in
              PRIM_PutChar(x) in
            let y = 1 in
            PRIM_AddInt(x,y) in
          let _ =
            let x = PRIM_CharChr(x) in
            PRIM_PutChar(x) in
          let y = 1 in
          PRIM_AddInt(x,y) in
        let _ =
          let x = PRIM_CharChr(x) in
          PRIM_PutChar(x) in
        let y = 1 in
        PRIM_AddInt(x,y) in
      let x =
        let x =
          let x =
            let x =
              let _ =
                let x = PRIM_CharChr(x) in
                PRIM_PutChar(x) in
              let y = 1 in
              PRIM_AddInt(x,y) in
            let _ =
              let x = PRIM_CharChr(x) in
              PRIM_PutChar(x) in
            let y = 1 in
            PRIM_AddInt(x,y) in
          let _ =
            let x = PRIM_CharChr(x) in
            PRIM_PutChar(x) in
          let y = 1 in
          PRIM_AddInt(x,y) in
        let x =
          let x =
            let _ =
              let x = PRIM_CharChr(x) in
              PRIM_PutChar(x) in
            let y = 1 in
            PRIM_AddInt(x,y) in
          let _ =
            let x = PRIM_CharChr(x) in
            PRIM_PutChar(x) in
          let y = 1 in
          PRIM_AddInt(x,y) in
        let _ =
          let x = PRIM_CharChr(x) in
          PRIM_PutChar(x) in
        let y = 1 in
        PRIM_AddInt(x,y) in
      let x =
        let x =
          let _ =
            let x = PRIM_CharChr(x) in
            PRIM_PutChar(x) in
          let y = 1 in
          PRIM_AddInt(x,y) in
        let _ =
          let x = PRIM_CharChr(x) in
          PRIM_PutChar(x) in
        let y = 1 in
        PRIM_AddInt(x,y) in
      let _ =
        let x = PRIM_CharChr(x) in
        PRIM_PutChar(x) in
      let y = 1 in
      PRIM_AddInt(x,y) in
    PRIM_CharChr(x) in
  PRIM_PutChar(x) in
let x = '\n' in
PRIM_PutChar(x)
