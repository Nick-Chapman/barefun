(library
 (name lib)
 (libraries unix))

(rule
  (with-stdout-to repl.ml
    (progn
      (echo "open Prim\n")
      (echo "type 'a my_list = Nil | Cons of 'a * 'a my_list\n")
      (cat ../example.fun)
      )))
